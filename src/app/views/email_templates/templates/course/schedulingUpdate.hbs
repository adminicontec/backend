<div class="img_icontec" style="display: block">
  <img src="{{params.mailer.mailer_image}}">
</div>

<div class="txt_icontec">
  <h2>Programación modificada</h2><br>
  {{#if params.course_name}}
    <div>El curso {{params.course_name}} del programa {{params.program_name}} con ID de servicio {{params.service_id}} ha sido modificado</div>
  {{else}}
  <div>El programa {{params.program_name}} con ID de servicio {{params.service_id}} ha sido modificado</div>
  {{/if}}
  <br>
  {{#each params.changes}}
    <div>{{{this.message}}}</div>
  {{/each}}
  <br>
  <div>En caso de cualquier inquietud registre un <a href="{{params.mailer.help_desk}}">nuevo ticket</a> en la <a href="{{params.mailer.help_desk}}">mesa de ayuda</a>, lo invitamos a consultar el <a href="{{params.mailer.help_desk}}/kb/faq.php?id=1#:~:text=Instructivo%20para%20LMS.pdf">instructivo de mesa de ayuda</a>. También dentro del campus puede acceder a soporte con la asistente virtual Daniela en la opción 2. <img src="{{params.mailer.virtual_help}}" style="width: 100px"></div>
  <br><br>
  <div>Cordialmente <br>Equipo Unidad Técnica de Educación</div>
  <img src="{{params.mailer.footer_icon}}">
</div>
